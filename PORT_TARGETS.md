# Port targets — `sha2-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `sha2` that codex-rs uses, and therefore the minimum subset that `sha2-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-135] (rank `23`, tier `direct`, status `To Do`)

**Cargo package name**: `sha2`
**Rust module name (in `use` statements)**: `sha2`

## Summary

- **3** distinct symbols imported by codex-rs
- **18** total `use` statements
- **10** codex source files affected
- **9** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `sha2::Sha256`

- **9 `use` site(s)** across **8 codex member crate(s)**: `codex-app-server`, `codex-app-server-transport`, `codex-cloud-requirements`, `codex-config`, `codex-external-agent-sessions`, …

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-transport/src/transport/auth.rs:15` (`codex-app-server-transport`) — `use sha2::Sha256`
- `app-server/tests/suite/v2/connection_handling_websocket.rs:27` (`codex-app-server`) — `use sha2::Sha256`
- `cloud-requirements/src/lib.rs:32` (`codex-cloud-requirements`) — `use sha2::Sha256`
- `config/src/fingerprint.rs:4` (`codex-config`) — `use sha2::Sha256`
- `external-agent-sessions/src/ledger.rs:6` (`codex-external-agent-sessions`) — `use sha2::Sha256`
- `login/src/auth/storage.rs:6` (`codex-login`) — `use sha2::Sha256`
- `login/src/pkce.rs:4` (`codex-login`) — `use sha2::Sha256`
- `rmcp-client/src/oauth.rs:35` (`codex-rmcp-client`) — `use sha2::Sha256`
- `secrets/src/lib.rs:14` (`codex-secrets`) — `use sha2::Sha256`

### `sha2::Digest`

- **8 `use` site(s)** across **7 codex member crate(s)**: `codex-agent-identity`, `codex-app-server-transport`, `codex-config`, `codex-external-agent-sessions`, `codex-login`, …
- Aliases used: `as _`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:30` (`codex-agent-identity`) — `use sha2::Digest as _`
- `app-server-transport/src/transport/auth.rs:14` (`codex-app-server-transport`) — `use sha2::Digest`
- `config/src/fingerprint.rs:3` (`codex-config`) — `use sha2::Digest`
- `external-agent-sessions/src/ledger.rs:5` (`codex-external-agent-sessions`) — `use sha2::Digest`
- `login/src/auth/storage.rs:5` (`codex-login`) — `use sha2::Digest`
- `login/src/pkce.rs:3` (`codex-login`) — `use sha2::Digest`
- `rmcp-client/src/oauth.rs:34` (`codex-rmcp-client`) — `use sha2::Digest`
- `secrets/src/lib.rs:13` (`codex-secrets`) — `use sha2::Digest`

### `sha2::Sha512`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-agent-identity`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `agent-identity/src/lib.rs:31` (`codex-agent-identity`) — `use sha2::Sha512`
